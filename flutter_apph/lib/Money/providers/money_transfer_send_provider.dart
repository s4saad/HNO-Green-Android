import '../../base_main_repo.dart';
import '../../transactions_screen_base_provider.dart';

class MoneyTransferSendProvider extends TransactionsScreenBaseProvider {
  MoneyTransferSendProvider(BaseMainRepository repo, List wallets)
      : super(repo, wallets);
}
